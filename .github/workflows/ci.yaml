name: nestjs-rabbitmq
on:
  [push]
  # pull_request:
  #   branches:
  #     - feat/publish-bulk
jobs:
  check-application:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm ci
      - run: npm test
